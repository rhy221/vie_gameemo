# Experiment: Strategy B (Face crop only, no context path)
# context tensor is zeroed in extract_features.py (strategy: face_only).
# Fusion still receives 4 tensors; attention branch learns to weight
# the zeroed context near 0. n_modalities stays 4.

visual_encoder:
  strategy: "face_only"

wandb:
  tags: ["ablation_strategy_b", "face_only"]
